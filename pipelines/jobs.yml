# This file selects the correct job definition based on system and buildType.
#
# Configuration values are capitalized because the Windows job uses one as the Visual Studio
# output directory, bin\Release or bin\Debug, and because the Release-only steps compare against
# 'Release'. Build preset names are lower case, so the uses that name a preset apply lower().

parameters:
  debug: 'false'

jobs:

- ${{ if eq(parameters.name, 'Windows') }}:
  - job: ${{ parameters.name }}
    displayName: ${{ parameters.name }}
    pool:
      vmImage: 'windows-latest'
    variables:
      # What the presets resolve their toolchain file against. Hosted agents do ship a vcpkg,
      # but they expose it as VCPKG_INSTALLATION_ROOT and it is an image snapshot that can
      # predate the baseline in vcpkg.json, so the bootstrap step clones the baseline instead
      # and this points at that clone.
      VCPKG_ROOT: $(Build.SourcesDirectory)/vcpkg
    steps:
    - template: windows.yml
      parameters:
        ${{ if eq(parameters.debug, 'true') }}:
          configuration: 'Debug'
        ${{ if eq(parameters.debug, 'false') }}:
          configuration: 'Release'

- ${{ if eq(parameters.name, 'WindowsArm64') }}:
  - job: ${{ parameters.name }}
    displayName: Windows ARM64 (cross-build)
    pool:
      # Azure Pipelines has no ARM64 Windows image, so this cross-builds from x64. The image
      # carries the MSVC ARM64 cross tools; windows-arm64.yml checks for them. The preset it
      # builds is win-vs2022-arm64, because the VS 2026 generator the win-vs2026-* presets ask
      # for needs a newer CMake than this image ships.
      vmImage: 'windows-2022'
    variables:
      VCPKG_ROOT: $(Build.SourcesDirectory)/vcpkg
      # This job has no CodeQL tasks of its own, and auto-injection would instrument vcpkg's
      # dependency build as well. The x64 job is where CodeQL runs.
      Codeql.SkipTaskAutoInjection: true
    steps:
    - template: windows-arm64.yml
      parameters:
        ${{ if eq(parameters.debug, 'true') }}:
          configuration: 'Debug'
        ${{ if eq(parameters.debug, 'false') }}:
          configuration: 'Release'

- ${{ if eq(parameters.name, 'Linux') }}:
  - job: ${{ parameters.name }}
    displayName: ${{ parameters.name }}
    pool:
      vmImage: 'ubuntu-latest'
    variables:
      VCPKG_ROOT: $(Build.SourcesDirectory)/vcpkg
    steps:
    - template: nix.yml
      parameters:
        presetPrefix: 'linux'
        ${{ if eq(parameters.debug, 'true') }}:
          configuration: 'Debug'
        ${{ if eq(parameters.debug, 'false') }}:
          configuration: 'Release'

- ${{ if eq(parameters.name, 'macOS') }}:
  - job: ${{ parameters.name }}
    displayName: ${{ parameters.name }}
    pool:
      vmImage: 'macos-latest'
    variables:
      VCPKG_ROOT: $(Build.SourcesDirectory)/vcpkg
    steps:
    - template: nix.yml
      parameters:
        # macos-latest is still the x86_64 image. If it becomes Apple silicon, switch this to
        # macos-arm64: those presets differ in CMAKE_OSX_ARCHITECTURES and vcpkg triplet, and
        # building the wrong one produces binaries the agent cannot run.
        presetPrefix: 'macos-x64'
        ${{ if eq(parameters.debug, 'true') }}:
          configuration: 'Debug'
        ${{ if eq(parameters.debug, 'false') }}:
          configuration: 'Release'
