# This defines a pipeline to build on Windows in Debug mode.

trigger:
  batch: true
  branches:
    include:
    - main

# See pipeline-CI-Debug-All.yml: CodeQL is a Release-only gate, and policy auto-injection would
# instrument vcpkg's dependency build and push the job past the agent timeout.
variables:
  Codeql.SkipTaskAutoInjection: true

stages:
  - stage: build
    displayName: Build
    jobs:
    - template: jobs.yml
      parameters:
        debug: true
        name: Windows
