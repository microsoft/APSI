parameters:
- name: configuration
  type: string
  values:
  - Debug
  - Release

steps:
- task: PowerShell@2
  displayName: 'Bootstrap vcpkg'
  inputs:
    targetType: 'inline'
    script: |
      $baseline = (Get-Content vcpkg.json | ConvertFrom-Json).'builtin-baseline'
      git clone https://github.com/microsoft/vcpkg.git
      cd vcpkg
      git checkout $baseline
      .\bootstrap-vcpkg.bat -disableMetrics
    workingDirectory: '$(Build.SourcesDirectory)'

- script: cmake --preset win-vs2022-x64
  workingDirectory: '$(Build.SourcesDirectory)'
  displayName: 'Configure APSI'

- ${{ if eq(parameters.configuration, 'Release') }}:
  - task: CodeQL3000Init@0

- script: cmake --build --preset win-vs2022-x64-${{ lower(parameters.configuration) }}
  workingDirectory: '$(Build.SourcesDirectory)'
  displayName: 'Build APSI'

- ${{ if eq(parameters.configuration, 'Release') }}:
  - task: CodeQL3000Finalize@0

- script: unit_tests.exe
  workingDirectory: '$(Build.SourcesDirectory)\out\build\win-vs2022-x64\bin\${{ parameters.configuration }}'
  displayName: 'Run unit tests'

# Integration tests are Release-only: a Debug build links vcpkg's unoptimized SEAL, which
# makes the homomorphic operations 10-20x slower and the suite impractical as a gate.
# The Large and Huge cases are DISABLED_ in the source, so only the small and medium ones
# run here. Add --gtest_also_run_disabled_tests before tagging a release.
- ${{ if eq(parameters.configuration, 'Release') }}:
  - script: integration_tests.exe
    workingDirectory: '$(Build.SourcesDirectory)\out\build\win-vs2022-x64\bin\${{ parameters.configuration }}'
    displayName: 'Run integration tests'

- ${{ if eq(parameters.configuration, 'Release') }}:
  - task: CredScan@3
    displayName: 'Credential Scan'

  - task: ComponentGovernanceComponentDetection@0
    displayName: 'Component Detection'
    inputs:
      scanType: 'Register'
      verbosity: 'Verbose'
      alertWarningLevel: 'High'

- task: PublishSecurityAnalysisLogs@3
  displayName: 'Publish Security Analysis Logs'
  inputs:
    ArtifactName: 'CodeAnalysisLogs'
    ArtifactType: 'Container'
    PublishProcessedResults: true
    AllTools: true
    ToolLogsNotFoundAction: 'Standard'
